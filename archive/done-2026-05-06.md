# Done — 06 May 2026

## Categories: DB-driven instead of hardcoded config

- Added `color` and `icon_svg` columns to `categories` table (migration + seed)
- Added `display_color` and `display_icon` accessors on `Category` model with safe fallbacks
- Added color/icon_svg fields to admin category form (`CategoryForm.php`)
- Replaced all `config('drinks')` lookups across the frontend with DB queries using `once()` memoization
- Updated: `venue-tile.blade.php`, `venues/show.blade.php`, `home.blade.php`, `discovery-page.blade.php`, `how-it-works.blade.php`
- Deleted `config/drinks.php` — no longer needed

## Hero section rework

- Replaced search input on homepage with 6 clickable category tiles (3x2 grid)
- Each tile links to discovery page pre-filtered by that category
- Styled with category-colored tinted backgrounds, 2px borders, bold labels
- Added prompt text: "Co pijesz dziś bez %?"
- Decorative hero markers now iterate over categories dynamically
- Tagline restyled as pill/badge

## Analytics infrastructure

- Created `venue_stats` table (migration) with daily counters: views, direction_clicks, website_clicks, instagram_clicks, shares, card_clicks
- Created `VenueStat` model with `track()` static method (firstOrCreate + increment pattern)
- Created `TrackController` at `POST /api/track` with throttle middleware
- Added `window.uniTrack()` JS helper in `layouts/app.blade.php` for fire-and-forget client-side tracking
- Added `stats()` HasMany relationship on `Venue` model

## Analytics event enrichment

- Enriched all PostHog events with `venue_name`, `venue_slug` where missing
- Added `uniTrack()` calls to: venue card clicks, navigate/directions clicks, website clicks, Instagram clicks, share clicks
- Server-side: `VenueController::show()` now calls `VenueStat::track()` for page views
- `DiscoveryPage` Livewire: category filter and district filter events now include names from DB

## Analytics dashboards (Filament)

### Owner panel (`/panel` dashboard)
- **New file:** `app/Filament/Panel/Widgets/VenueAnalyticsWidget.php`
- **New file:** `resources/views/filament/panel/widgets/venue-analytics.blade.php`
- Period selector (7d / 30d / All) — reactive via Livewire
- 6-stat grid with icons: views, card clicks, directions, website, Instagram, shares
- Bar chart showing daily views over selected period
- Only visible when user has a claimed venue
- Registered in `PanelPanelProvider`

### Admin panel (`/admin/venues/{id}/edit`)
- **New file:** `app/Filament/Resources/Venues/Widgets/VenueAnalyticsWidget.php`
- **New file:** `resources/views/filament/resources/venues/widgets/venue-analytics.blade.php`
- Footer widget on venue edit page
- Period selector (7d / 30d / All)
- 6-stat summary grid
- Daily breakdown table (last 14 days, all metrics)
- Wired into `EditVenue.php` via `getFooterWidgets()`

## Bug fix

- Renamed `VenueStat::increment()` → `VenueStat::track()` — the static method conflicted with Eloquent's built-in `increment()` instance method, causing a fatal error
- Updated all callers: `VenueController.php`, `TrackController.php`

## Test status

- All 262 tests passing (384 assertions)
- Pint formatting clean

## Still pending

- Run `npm run build` to compile frontend changes (category tiles, hero rework, uniTrack calls)
- No new tests written for the analytics widgets yet
